やること: 今後作成するページのフレームの作成

1. 以下の構成を把握する
```
### Step0. STM32の開発環境を作ろう

#### 0. STM32とは

- マイコンとはArudinoとの差について

#### 1. 環境構築

- CubeIDEとTeraTermを入れる

### Step1. プログラムの書き方を知ろう

#### 0. プロジェクトを作成しよう

- プロジェクト作成とwrapperの作成方法を教える


#### 1. LEDをつけてみよう

- LED点滅のサンプルを見せる
    - 点滅速度を変えてもらう


### Step2. PCと文字のやりとりをしてみよう

#### 1. PCに文字を送ってみよう

- ポーリング送信をする
    - ASCIIと文字形式を載せて、文字→数字（送信）→文字（受信）の流れを知ってもらう
    - std::to_string()で、カウンターを作る

#### 2. PCから文字を受け取ってみよう

- ポーリング受信をする
    - 小文字⇔大文字変換したものを返送
    - 関数の実行中しか受信できないことを知る

#### 3. 受信の取りこぼしをなくそう

- 割り込み受信を教える

#### 4. 便利なprintf()を使ってみよう

- printf()の使い方を教える
- 桁数制限や符号出力もできるようにする

### Step3 操縦装置（プロポ）を使ってコントロールしよう

#### 1. 信号の流れとひこロボ自動投下の話

- 図とか映像を見せてあげたい

#### 2. SBUSの読み取り

- UARTDMAでSBUSを読み取る

- 飛行機とマルチコプターのチャンネル割り当て例を示す
    - 6ch程度の読み取りと出力のみ

#### 3. サーボモーター

- pwmにいろいろな値を入れて開閉ができれば〇
- 投下装置を貸してあげたい

#### 4. 赤外線の検出

- 12bitadcの値を読む
    - 太陽光の値も調べて閾値のヒントを得る


#### 5. 自動投下装置の設計

- 手動・自動投下に両対応する
    - アルゴリズム入門レベル

### Step4. センサーを使って距離を測ってみよう

- 制御をするうえで、目標や床との距離を測ることはとても大切

#### 1. センサーとの通信方法

- センサーのレジスタの概念を伝える
- 電源レジスタとデータレジスタ16bitを持つデモコードを乗せる

#### 2. 超音波センサーで距離を測ってみよう

- 超音波センサーで距離[mm]を得る

- 原理自体は、雷の音と距離なので導入として最適
- レジスタ構造も簡単ですごくいい
- センサー5個あってちょっと壊れてもノーダメージ


#### 3. ToFセンサーで距離を測ってみよう

- ToFセンサーで距離[mm]を取得する

- VL53C4はあまりにも難しすぎる（STM32のミドルウェアが絡むため）
    - レジスタ構成が簡単なセンサーを購入したい

#### 4. 2つのセンサーを使って正確な距離を測ろう

- いい感じに平均値取ったりして距離が確定しているものに対して±0.5cmくらいを出す

### Step5. センサーを使って角度を測ってみよう

機体が傾きすぎると墜落することになるので、水平に保つために角度を知る必要がある

#### 0. SPI通信について

- SPIのほうが安定するので、ここはSPIでやりたい

#### 1. 加速度データの取得

- ICM42688P / ICM45686 の最小レジスタ構成を教える

#### 2. 加速度のベクトルから角度を計算

- 重力加速度の分力をもとめる計算の逆なのでわかりやすそう
    - 重力以外がかかると大変なことになるね

#### 3. 角速度データの取得

- ICM42688P / ICM45686 の最小レジスタ構成を教える

#### 4. 角速度の積分から角度を計算

- 速度→位置の積分と同じなのでよさそう
    - 数値積分（台形近似）を教えたい
    - ねじれると大変なことになるね

#### 5. 相補フィルター

- それぞれのいいところどりを簡単にする
    - k = 0.98でとりあえずいいや

#### 6. Madgwickフィルター

- ライブラリを持ってきて値の入出力を試してみる
    - 係数を 0.05くらいにしておく


### Step6. センサーのライブラリを自作してみよう

#### 0. ライブラリとは

- すべてのセンサーのレジスタ把握したい、、？
- ライブラリがあれば、細かいことを知らなくても使える

#### 1. クラスとは

- 関数や変数をまとめたもの

#### 2. IMUのライブラリを書いてみよう

- 3つのメソッドを持つ小さなライブラリを作成
```cpp
SensorStart(), GetAccel(), GetGyro()
```

- HALの関数を直書きする(ここではI2Cのみ)

#### 3. ピンを設定できるようにしよう

- コンストラクタでピン設定(I2Cのみ)

#### 4. I2C/SPI両対応なコードを書こう(1)

- コンストラクタのオーバーロード
    - フラグ管理
    - 条件付きコンパイル

#### 5. I2C/SPI両対応なコードを書こう(2)

- 派生クラスでのオーバーロード
    - 仮想関数と継承
    - HAL_I2C / HAL_SPIクラス

#### 6. 地磁気センサーのライブラリ（演習問題）

- BM1422AGMVのライブラリを書く
- I2Cのみのため、前半の復習

#### 7. ICM42688Pのライブラリを書く（演習問題）

- ICM42688Pのライブラリを書く
- I2C/SPI両対応のため、後半の復讐

### Step7. プログラムの設計を学ぼう

#### 0. 設計の重要性

- 大変になったコードでも見せる

#### 1. 命名規則と名前空間

- 命名の統一が重要な理由
- 名前の衝突をさけよう

#### 2. 関数化とファイルの分割

- 長い処理をまとめて、隔離しちゃおう
- クラスじゃなくてもヘッダーとソースが使えるよ
    - inline関数も教えたい

#### 3. マジックナンバーのenum class化

- 関数のreturnや設定値に意味を持たせよう

#### 4. 状態遷移の設計(1)

- StatePattern（if文による遷移）

#### 5. 状態遷移の設計(2)

- StatePattern（スマートポインタによる遷移）

```

2. pages/documnetにstepのフォルダを作成

3. 各stepフォルダ内に01_(簡単な講座名)のフォルダを作成

4. routes/documents/step{i}を作成する

5. 3で作成したフォルダをすべて登録

6. すでにあるsection1 2 を参考に3で作成したフォルダ内に本文０のページを作成する(.tsxファイル）